
@model IEnumerable<Site.Models.Menubar>


    <ul id="main-nav" class="nav nav-tabs nav-stacked" style="">
        <li class="active">
            <a href="#">
                <i class="glyphicon glyphicon-th-large"></i>
                首页
            </a>
        </li>
        @foreach (var s in Model)
        {
            if (s.ParentId == 0)
            {
                <li>
                    <a href="#@s.MenuId" class="nav-header collapsed text-center" data-toggle="collapse">
                        <i class="glyphicon glyphicon-cog"></i>
                        @s.MenusNam
                        <span class="pull-right glyphicon glyphicon-chevron-down"></span>
                    </a>


                    <ul id="@s.MenuId" class="nav nav-list collapse secondmenu" style="height: 0px;">

                        @foreach (var t in Model)
                    {
                        if (t.ParentId == s.MenuId)
                        {
                                <li><a href="#"><i class="glyphicon glyphicon-user"></i>@t.MenusNam</a></li>
                            }
                        }


                    </ul>
                </li>
                
                @*<div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse"
                               href="#@s.MenuId">
                                @s.MenusNam
                            </a>
                        </h4>
                    </div>
                    <div id="@s.MenuId" class="panel-collapse collapse in">
                        @foreach (var t in Model)
                    {
                        if (t.ParentId == s.MenuId)
                        {
                                <ul class="btn btn-block btn-lg   btn-primary">@t.MenusNam</ul>
                            }
                        }
                    </div>
                </div>*@
            }

        }</ul>






